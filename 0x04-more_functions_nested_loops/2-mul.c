#include "main.h"

/**
 * mul - function to multiply two integers
 * @a: integer 1
 * @b: integer 2
 * Return: returns the multiplication of the integers
 */
int mul(int a, int b)
{
	return (a * b);
}
