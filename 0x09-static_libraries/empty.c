#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *s = "I am a bad guy lol";
	int a = _strlen(s);
	printf("%d\n", a);

	return (0);
}
