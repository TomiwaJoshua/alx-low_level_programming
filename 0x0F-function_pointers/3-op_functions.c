#include "3-calc.h"

/**
 * op_add - adds two numbers
 * @a: first number
 * @b: second number
 * Return: returns the result of the arithmetic operation
 */
int op_add(int a, int b)
{
	return (a + b);
}
#include "3-calc.h"

/**
 * op_sub - subtracts two numbers
 * @a: first number
 * @b: second number
 * Return: returns the result of the arithmetic operation
 */
int op_sub(int a, int b)
{
	return (a - b);
}
#include "3-calc.h"

/**
 * op_mul - multiplies two numbers
 * @a: first number
 * @b: second number
 * Return: returns the result of the arithmetic operation
 */
int op_mul(int a, int b)
{
	return (a * b);
}
#include "3-calc.h"

/**
 * op_div - divides two numbers
 * @a: first number
 * @b: second number
 * Return: returns the result of the arithmetic operation
 */
int op_div(int a, int b)
{
	return (a / b);
}
#include "3-calc.h"

/**
 * op_mod - finds remainder of division of two numbers
 * @a: first number
 * @b: second number
 * Return: returns the result of the arithmetic operation
 */
int op_mod(int a, int b)
{
	return (a % b);
}
